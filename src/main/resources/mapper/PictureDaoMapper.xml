<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.studio.yishujutan.dao.PictureDao">

<select id="getUserIconUrl" resultType="String" parameterType="String">
    select pic_link from picture where pic_object = 'user' and object_id = #{user_id}
</select>

</mapper>